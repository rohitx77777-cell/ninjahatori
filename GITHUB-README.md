# Eli Kids Website

GitHub-ready source package for the Eli Kids website.

## Important
Environment variables/secrets are intentionally excluded. If the application needs values from `.env`, add them locally and configure the same variables in Vercel Project Settings → Environment Variables.

Generated caches, dependencies, build output, and test artifacts are also excluded.
